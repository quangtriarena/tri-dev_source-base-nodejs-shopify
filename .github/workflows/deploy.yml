name: Deploy Node.js App

on:
    push:
        branches:
            - master

jobs:
    deploy:
        runs-on: ubuntu-latest

        steps:
            - name: Install ssh key
              run: |
                  echo "${{ secrets.SSH_PRIVATE_KEY}}"" > ~/.ssh/id_rsa
                  ssh-keyscan -H ${{ secrets.HOST }} >> ~/.ssh/known_hosts

            - name: Connect server and pull code
